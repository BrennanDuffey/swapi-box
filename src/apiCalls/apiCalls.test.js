import { fetchFilms, fetchPeople, fetchPlanets, fetchVehicles, fetchURL } from './apiCalls';

desribe('fetchFilms', () => {
  let mockFilmsResponse;
  let mockPeopleResponse;
  let mockPlanetsResponse;
  let mockVehiclesResponse;
})