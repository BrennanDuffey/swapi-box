import React from 'react';

const Card = (props) => {
  const selectCardType = (props) => {
    let planetName;
    let population;
    let species;
    if (props.selected === "people") {

    }
  }
  return (
    <article>
      <h3>{props.name}</h3>
    </article>
  )
}

export default Card